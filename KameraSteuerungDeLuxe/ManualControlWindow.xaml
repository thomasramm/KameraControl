<Window x:Class="KameraSteuerungDeLuxe.ManualControlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KameraSteuerungDeLuxe"
        mc:Ignorable="d"
        Height="200"
        Width="150"
        WindowStyle="None"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance Type=local:ManualControlViewModel}"
        Title="Kamera Steuerung"
        Background="#222"
        MouseDown="Window_MouseDown"
        Closing="Window_Closing">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#226084" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="FontSize" Value="32" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#bee6fd" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#c94453" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#1a1a1a" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Grid.Row="1"
              ShowGridLines="False"
          Background="#1e1e1e">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" Grid.Column="1" Content="▲" Command="{Binding UpCommand}" />
        <Button Grid.Row="1" Grid.Column="0" Content="◀" Command="{Binding LeftCommand}" />
        <Button Grid.Row="1" Grid.Column="2" Content="▶" Command="{Binding RightCommand}" />
        <Button Grid.Row="2" Grid.Column="1" Content="▼" Command="{Binding DownCommand}" />
        <Button Grid.Row="3" Grid.Column="0" Command="{Binding ZoomOutCommand}">
            <Image Source="/images/minus_128x128.png" Width="30" Height="30" />
        </Button>
        <TextBlock Grid.Row="3" Grid.Column="1"
                   Text="Zoom"
                   Foreground="White"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   FontSize="16" />
        <Button Grid.Row="3" Grid.Column="2" Command="{Binding ZoomInCommand}">
            <Image Source="/images/plus_128x128.png" Width="30" Height="30" />
        </Button>
    </Grid>
</Window>